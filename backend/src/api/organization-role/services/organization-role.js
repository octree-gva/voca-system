'use strict';

/**
 * organization-role service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::organization-role.organization-role');
