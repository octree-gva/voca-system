'use strict';

/**
 *  jelastic-config controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::jelastic-config.jelastic-config');
